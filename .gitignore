# Build output
/dist/
/coact
/coact.exe
*.test

# Go
/vendor/

# This repo is coact's source, not a project that uses coact. Ignore any
# artifacts from dogfooding `coact init` in the repo root.
/.coact/
/CLAUDE.md
/AGENTS.md
/ANTIGRAVITY.md
/.claude/
/.mcp.json

# Editor / OS
.DS_Store
*.swp
